# Maple 082 个人网页复刻

这是一个无需安装依赖的静态网页，入口是 `index.html`。

本地预览：

```powershell
python -m http.server 4173
```

然后打开 `http://127.0.0.1:4173/index.html`。

主要修改位置：

- `index.html`：文字、链接和页面结构
- `styles.css`：颜色、排版、响应式布局
- `script.js`：菜单与浮层清理逻辑
- `public/assets/hero-portrait.png`：头像素材
